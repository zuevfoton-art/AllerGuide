# Клинические функции AllerGuide (ориентиры РААКИ)

Спецификация улучшений по мотивам [сборника клинических рекомендаций РААКИ](https://raaci.ru/dat/pdf/akd.pdf). Документ описывает реализованные возможности, принципы UX и границы ответственности приложения.

## Практичный порядок внедрения

Клинические улучшения внедряются поэтапно — от экстренного сценария к специализированным модулям дневника.

| Приоритет | Фокус | Статус | В main |
|-----------|-------|--------|--------|
| **1** | Цифровой паспорт SOS, мини-шкалы, связка триггеров, отчёт v2 (§1–§4) | ✅ Готово | #37 |
| **2** | Дневник по РААКИ: условия профиля, рекомендуемые шкалы, тренды, ПСВ/АСИТ по профилю | ✅ Готово | #42 |
| **3** | АСИТ: курс, карточка, расширенная запись, сводка в PDF (§5) | ✅ Готово | #42 |
| **4** | Пищевая и лекарственная аллергия: реестр, предупреждения ЛС, карточки, PDF (§6) | ✅ Готово | #42 |
| **5** | Инсектная аллергия, напоминания АСИТ, расширенный OCR сканера | ✅ Готово | — |

> Порядок мержа в `main`: **#39** (design-mockup) → **#42** (P2–P4). PR #40 и #41 закрыты как дубликаты #42.

### Что ещё в бэклоге (внутри приоритетов 1–4)

| Элемент | Статус |
|---------|--------|
| Блок `triggerContext` в PDF-отчёте | ✅ |
| UAS7 в рекомендуемых шкалах (крапивница в профиле) | ✅ |
| Перекрёстные реакции в префилле «Питание» | ✅ |
| Mobile unit-тесты (SOS, дневник, отчёт) | ⏳ Roadmap P2.2 |
| `design-mockup.html` — клинические экраны дневника и отчёта | ✅ |

> Инженерный roadmap продакшена (EAS, backend, compliance) — в [`roadmap-to-prod.md`](./roadmap-to-prod.md).

## Принципы UX

1. **Progressive disclosure** — на экране SOS и в дневнике по умолчанию показывается компактная сводка; детали раскрываются по запросу.
2. **Один чип «Шкала»** вместо четырёх отдельных разделов — выбор типа шкалы внутри мастера.
3. **Автоподстановка контекста** в разделе «Триггер» (пыльца, сканы, ЛС) без лишних полей на главном экране.
4. **Информационный характер** — приложение не ставит диагноз и не назначает лечение.

## 1. Цифровой паспорт аллергика (SOS)

### Данные паспорта

| Поле | Описание |
|------|----------|
| Аллергены | Из профиля (read-only на SOS) |
| Непереносимые ЛС | Список препаратов, введённый пользователем |
| Триггеры | Пыльца, пища, животные и др. |
| Адреналин | Марка, срок годности, где хранится |
| Врач | Имя и телефон лечащего аллерголога |
| История анафилаксии | Флаг «была тяжёлая реакция» |
| Набор первой помощи | Чеклист предметов |

Хранение: `app_settings` → ключ `sosPassport:{profileId}` (JSON, версия `v: 1`).

### Алгоритм анафилаксии (справочно)

| Степень | Признаки | Ориентиры действий |
|---------|----------|-------------------|
| 1 | Кожа/слизистые | Антигистаминное по назначению врача, наблюдение |
| 2 | + дыхание, ЖКТ, лёгкие сердечно-сосудистые | Адреналин по плану врача, вызов 103 |
| 3 | Шок, обструкция дыхания | Адреналин немедленно, 103, положение на спине с приподнятыми ногами |

**Бифазная реакция:** предупреждение о возможном повторном ухудшении в течение 4–6 часов — показывается на экране SOS при раскрытии блока анафилаксии.

### Экспорт

- Текстовая сводка для медработника (кнопка «Поделиться»).
- PDF через `expo-print` (тот же механизм, что отчёт для врача).

## 2. Мини-шкалы в дневнике

Отдельный раздел **«Шкала»** — опциональный, не входит в полный мастер записи по умолчанию.

Шкалы подбираются по профилю (`diary-profile.ts`): ринит/поллиноз → ARIA-lite, астма → ACT, атопический дерматит → SCORAD-lite, **крапивница в аллергенах профиля** → UAS7. Типы состояний задаются в профиле и дополняются эвристикой по аллергенам. Разделы «Пикфлоуметрия» и «АСИТ» показываются только при соответствующих состояниях (FR-PROF-10/11). UAS7 входит в тренды дневника (`DIARY_TREND_SCALE_IDS`).

### ARIA-lite (ринит)

4 симптома по шкале 0–3: заложенность, ринорея, чихание, зуд. Сумма 0–12 → интерпретация: лёгкая / умеренная / выраженная.

### ACT (астма)

5 вопросов по шкале 1–5. Сумма 5–25:

- ≥ 20 — хороший контроль
- 16–19 — частичный контроль
- ≤ 15 — неконтролируемая астма (консультация врача)

### SCORAD-lite (атопический дерматит)

Упрощённая оценка: площадь поражения (%), зуд 0–10, нарушение сна 0–3. Приближённый балл для тренда, не заменяет клинический SCORAD.

### UAS7 (крапивница, дневная запись)

Число элементов сыпи (0 / 1–6 / 7–12 / >12) и зуд 0–3. Для недельного UAS7 врач агрегирует записи из отчёта.

### Валидация

`validateClinicalScale(scaleId, answers)` в `packages/core/src/clinical-scales.ts` — проверка обязательных полей и диапазонов перед сохранением записи дневника.

## 3. Связка дневника с триггерами

При открытии раздела «Триггер» автоматически подставляется контекст (`buildTriggerPrefill`):

| Источник | Поле дневника | Данные |
|----------|---------------|--------|
| Wellness / Open-Meteo | `pollenContext` | Уровень пыльцы по профильным аллергенам |
| Сканер | `recentScan` | Последнее сканирование за 24 ч |
| Дневник «Лекарство» | `todayMeds` | Препараты, принятые сегодня |

Пользователь может отредактировать или очистить подставленные значения.

## 4. Отчёт для врача v2

Новые блоки в `DOCTOR_REPORT_BLOCKS`:

| Блок | Содержание |
|------|------------|
| `scales` | Записи «Шкала» с итоговым баллом и интерпретацией |
| `triggerContext` | Триггеры с контекстом пыльцы, сканов и ЛС (агрегированный блок PDF) |

Дополнительно в PDF:

- **Тренд ПСВ** — min / max / последнее значение за период.
- **Сводка шкал** — последние оценки по каждому типу.
- **Сводка АСИТ** — курс и комплаенс (при включённом блоке).
- **Пищевая и лекарственная аллергия** — сводка исключений и реакций.
- **Контекст триггеров** — агрегат записей с пыльцей, сканами и ЛС.
- **Паспорт SOS** — краткая выжимка (аллергены, непереносимые ЛС, адреналин).

## 5. АСИТ (аллерген-специфическая иммунотерапия)

### Курс АСИТ

Пользователь задаёт курс, назначенный врачом (хранение: `app_settings` → `asitCourse:{profileId}`):

| Поле | Описание |
|------|----------|
| Аллерген | Целевой аллерген курса |
| Препарат | Торговое название |
| Путь | SLIT (подъязычная) / SCIT (подкожная) |
| Фаза | Наращивание / поддерживающая |
| Схема | Текстовое описание по назначению врача |

### Запись приёма в дневнике

Расширенный раздел «АСИТ»: аллерген, препарат, путь, фаза, дата/время, номер дозы, соблюдение графика, местная и системная реакция. При активном курсе поля **автозаполняются** из настроек.

### Сводка и отчёт

- Карточка **АСИТ** на экране дневника: приёмы за 30 дней, в срок / пропущено
- PDF-отчёт: блок **«Сводка АСИТ»** с курсом и комплаенсом

### Границы

Приложение **не назначает и не корректирует** дозы АСИТ (`ASIT_DISCLAIMER`).

## 6. Пищевая и лекарственная аллергия

### Реестр пищевых исключений

Пользователь дополняет аллергены профиля списком исключений (хранение: `app_settings` → `foodDrugRegistry:{profileId}`):

| Поле | Описание |
|------|----------|
| Дополнительные исключения | Продукты сверх аллергенов профиля |
| Заметки для врача | Строгость диеты, переносимость следов |

Непереносимые ЛС хранятся в **паспорте SOS** (`drugIntolerances`) и используются для предупреждений.

### Записи в дневнике

**Питание** — источник (сканер / вручную), автоподстановка из последнего сканирования за 24 ч (с объединением аллергенов профиля), **перекрёстные реакции** из справочника (`buildFoodPrefill`), тип клинической реакции (ораллергический синдром, ЖКТ, кожа, дыхание, анафилаксия).

**Лекарство** — при вводе названия препарата проверяется совпадение с непереносимыми ЛС из SOS; при совпадении показывается предупреждение. Отдельно фиксируется тяжесть побочной реакции.

### Карточки на экране дневника

- **Пищевая аллергия** — при типе `food` или пищевых аллергенах в профиле: список исключений, KPI за 30 дней
- **Лекарственная аллергия** — при типе `drug` или непереносимых ЛС в SOS: список препаратов, предупреждения за 30 дней

### Отчёт для врача

PDF-отчёт: блок **«Пищевая и лекарственная аллергия»** — сводка исключений, непереносимых ЛС, записей о питании и лекарствах за период.

### Границы

Приложение **не назначает диету и не отменяет препараты** (`FOOD_DRUG_DISCLAIMER`).

## 7. Инсектная аллергия, напоминания АСИТ, OCR

### Инсектная аллергия

План действий при укусах (хранение: `app_settings` → `insectActionPlan:{profileId}`):

| Поле | Описание |
|------|----------|
| Дополнительные насекомые | Сверх аллергенов профиля |
| Где хранится адреналин | Ориентир для экстренного доступа |
| План действий | Шаги по назначению врача |
| Заметки для врача | Переносимость, носимый адреналин |

Раздел дневника **«Укус насекомого»** — тип насекомого, место укуса, тяжесть, местные/системные симптомы, использование адреналина. При открытии раздела поля **автозаполняются** из профиля и плана.

Карточка **Инсектная аллергия** на экране дневника (при типе `insect` или инсектных аллергенах): список рисковых насекомых, KPI за 30 дней.

PDF-отчёт: блок **«Инсектная аллергия»** — план, сводка укусов за период.

### Напоминания АСИТ

В настройках курса АСИТ — ежедневное push-напоминание (`reminderHour` / `reminderMinute` в `asitCourse:{profileId}`). На native планируется через `expo-notifications`; на web недоступно.

### Расширенный OCR сканера

Режимы **меню**, **лекарство**, **косметика** используют OCR-пайплайн (`packages/ai/src/ocr.ts`):

- нормализация распознанного текста;
- извлечение блока «состав» / «действующее вещество»;
- демо-режим при отсутствии нативного OCR (с ручной правкой текста перед проверкой).

Результат сканирования помечается `source: 'ocr'`.

## 8. Границы и дисклеймеры

- Все шкалы — **самоотчёт** пользователя/родителя.
- Алгоритм анафилаксии — **общеинформационный**, план действий задаёт врач.
- Отчёт PDF не является медицинской документацией (см. `DOCTOR_REPORT_DISCLAIMER`).

## 9. Файлы реализации

| Компонент | Путь |
|-----------|------|
| Паспорт (домен) | `packages/core/src/allergy-passport.ts` |
| Шкалы | `packages/core/src/clinical-scales.ts` |
| Профиль дневника (РААКИ) | `packages/core/src/diary-profile.ts` |
| Условия профиля (mobile) | `apps/mobile/src/services/profile-conditions-service.ts` |
| Контекст триггеров | `packages/core/src/diary-triggers.ts` |
| SOS UI | `apps/mobile/app/(tabs)/sos.tsx`, `sos-edit.tsx` |
| SOS сервис | `apps/mobile/src/services/sos-passport-service.ts` |
| АСИТ (домен) | `packages/core/src/asit-therapy.ts` |
| Курс АСИТ (mobile) | `apps/mobile/src/services/asit-course-service.ts`, `app/asit-course.tsx` |
| Пищевая/лекарственная (домен) | `packages/core/src/food-drug-allergy.ts` |
| Инсектная аллергия (домен) | `packages/core/src/insect-allergy.ts` |
| OCR сканера | `packages/ai/src/ocr.ts` |
| План при укусах (mobile) | `apps/mobile/src/services/insect-action-plan-service.ts`, `app/insect-action-plan.tsx` |
| Напоминания АСИТ (mobile) | `apps/mobile/src/services/asit-reminder-service.ts` |
| Реестр исключений (mobile) | `apps/mobile/src/services/food-drug-registry-service.ts`, `app/food-drug-registry.tsx` |
| Дневник | `apps/mobile/app/(tabs)/diary.tsx` |
| Отчёт | `apps/mobile/src/services/doctor-report-service.ts` |

## 10. Тестирование

- `packages/core/src/clinical-scales.test.ts` — валидация и подсчёт баллов.
- `packages/core/src/allergy-passport.test.ts` — сериализация паспорта, текст экспорта.
- `packages/core/src/asit-therapy.test.ts` — курс, префилл, комплаенс, отчёт.
- `packages/core/src/food-drug-allergy.test.ts` — реестр, непереносимость ЛС, сводки, отчёт.
- `packages/core/src/insect-allergy.test.ts` — план, префилл, сводки, отчёт.
- `packages/ai/src/ocr.test.ts` — нормализация OCR, извлечение состава.
- `packages/core/src/diary-triggers.test.ts` — префилл триггеров и отчёт контекста.
