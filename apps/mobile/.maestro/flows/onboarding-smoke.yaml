# P2.1a — onboarding: register → intro skip → profile → home
appId: com.aclearo.app
---
- runFlow: _offline-bootstrap.yaml
- assertVisible:
    id: tab-home
- assertVisible:
    id: tab-diary
- assertVisible:
    id: tab-scanner
- assertVisible:
    id: tab-map
- assertVisible:
    id: tab-sos
# North-star N1: four navigation tabs + SOS control, market moved to the profile hub.
- assertNotVisible:
    id: tab-market
