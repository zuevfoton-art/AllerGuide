# P2.1a — onboarding: register → intro skip → profile → home
appId: com.allerguide.app
---
- runFlow: _offline-bootstrap.yaml
- assertVisible:
    id: tab-home
- assertVisible:
    id: tab-diary
- assertVisible:
    id: tab-scanner
- assertVisible:
    id: tab-sos
