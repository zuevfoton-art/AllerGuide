# Fold IME on the scanner without BACK / hideKeyboard.
# Zip UI puts a 280px camera well above the title, so scanner-title
# scrolls off when Gboard is open (nightly 35498846516). Tap the camera
# hint — it stays on screen above the manual field.
appId: com.aclearo.app
---
- tapOn:
    id: scanner-camera-hint
