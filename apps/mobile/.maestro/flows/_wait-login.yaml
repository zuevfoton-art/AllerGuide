# First painted auth chrome (toggle sits above the phone field / hero fold).
# Do not wait for auth-login-input — it can sit below AuthHero on Pixel 6.
appId: com.aclearo.app
---
- hideKeyboard
- extendedWaitUntil:
    visible:
      id: auth-mode-phone
    timeout: 120000
