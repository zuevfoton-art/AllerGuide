# Shared subflow: staging register → onboarding → home, hints dismissed.
# First-run profile wizard is 5 screens (same as `_offline-bootstrap.yaml`).
appId: com.aclearo.app
---
- runFlow: _staging-bootstrap-until-home.yaml
- runFlow: _dismiss-hints.yaml
